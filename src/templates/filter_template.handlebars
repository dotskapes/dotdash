<div id="options">
    <h2>Filter</h2>
    {{#each filters}}
        <div class="filter">
            <div class="collapse-toggler">
                <span class="expand-arrow">&#9655;</span>
                <span class="collapse-arrow">&#9661;</span>
                {{title}}
            </div>
            <div id="{{name}}-filter" class="collapsible" style="display: block;">
              {{#if radioSubGroups}}
                {{#each options}}
                  {{#each subOptions}}
                    <label>
                      <input class="filter-button {{../name}}" type="radio"
                      name="{{../name}}" value="{{@index}}"
                      {{#unless @index}}checked{{/unless}} />{{label}}
                    </label>
                  {{/each}}
                  <br>
                {{/each}}
              {{else}}
                {{#each options}}
                  <label>
                    <input class="filter-button {{../name}}" type="radio"
                    name="{{../name}}" value="{{@index}}"
                    {{#unless @index}}checked{{/unless}} />{{label}}
                  </label>
                {{/each}}
              {{/if}}
            </div>
        </div>
    {{/each}}
</div>
